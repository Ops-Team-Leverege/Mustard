/**
 * RAG SQL Queries
 * 
 * Purpose:
 * Contains SQL query builders for RAG retrieval operations.
 * Kept separate to allow query reuse and testing.
 * 
 * Layer: RAG (queries)
 */

export function lastMeetingQuery(companyId: string) { ... }
